#include <iostream.h>

void main()
{
	char s[50];
	s[0] = '1';
	s[1] = '2';
	s[2] = '3';
	s[3] = '4';
	


	cout<<"s="<<s<<endl;          //此时s是指针，是字符数组第一个元素的地址，当用s时，这是字符串的特殊功能， 不返回地址，而是
	                              // 字符串， 若字符数组中没有'\0', thing get mess.
	                              // 当成字符数组时，没有要求，若当成字符串时，必须有'\0'.
	//for (int i = 0; i < 4; i++)
	//{
	//	cout<<s[i]<<endl;
	//}
}
